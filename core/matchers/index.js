var templateMatcher = require('./templateMatcher');
var quizMatcher = require('./quizMatcher');
var shopMatcher = require('./shopMatcher');

exports.list = [templateMatcher, quizMatcher, shopMatcher];
